name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  check:
    name: Master's thesis Poc
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
      RUST_LOG: INFO
      RUST_BACKTRACE: FULL
    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Install latest stable Rust
        uses: dtolnay/rust-toolchain@stable

      - name: rust version
        run: rustc --version

      - name: install foundry
        uses: onbjerg/foundry-toolchain@v1
        with:
          version: nightly

      - name: forge version
        run: forge --version

      - name: rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: organisation

      - name: cargo build
        working-directory: ./organisation
        run: cargo check

      - name: cargo test
        working-directory: ./organisation
        run: cargo test

      - name: cargo build examples
        working-directory: ./organisation
        run: cargo build --examples

      - name: forge tests verbose
        working-directory: ./blockchain
        run: >
          forge test -vvvvv
          --root=blockchain

      - name: run local anvil node and deploy smart contract
        run: >
          docker-compose up -d --build && 
          docker wait masters-blockchain &&
          docker logs masters-anvil &&
          docker logs masters-blockchain

      - name: cargo run start-the-world
        working-directory: ./organisation
        run: >
          cargo run --example start-the-world

      - name: curl
        run: curl localhost:8545 || echo "pinging localhost 8545"

      - name: integration tests
        working-directory: ./organisation
        run: cargo integration-tests

      - name: tear down local anvil node
        run: docker-compose down
